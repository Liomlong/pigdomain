import React from 'react';
import { TonConnectButton } from '@tonconnect/ui-react';

const TonConnect = () => {
    return (
        <div>
            <TonConnectButton />
        </div>
    );
};

export default TonConnect;
